# 功能模块提示词汇总文档

> 本文档汇总了项目中所有功能模块的设计实现提示词
>
> **生成日期**: 2026-06-01
> **项目路径**: c:\Users\wangmingyu\Desktop\work\day5\cnAgentOS

---

## 一、后台管理框架模块

### 1.1 后台登录页

**功能**: 管理员专用登录入口，采用企业化管理软件风格

**实现流程**:
1. 设计响应式、沉浸式、自适应登录界面
2. 界面风格以企业化管理软件为主，简约专业
3. 默认登录用户名为admin/admin888
4. 验证成功后跳转至后台主页

**技术考虑**:
- 采用Layui框架开发后台经典管理界面
- 左侧为菜单，右侧为工作区
- 后台主页预留页面，后期开发完所有模块后再实现

**细节考虑**:
- 后台登录页为管理员admin专用
- 界面风格与管理侧保持一致

---

### 1.2 用户管理

**功能**: 实现用户的新增、删除、批量删除、修改、分页等功能

**实现流程**:
1. 用户列表分页显示（每页20条）
2. 支持新增用户（用户名、密码、角色等）
3. 支持删除/批量删除用户
4. 支持修改用户信息
5. 增加角色设置列显示角色信息

**技术考虑**:
- admin用户为默认系统用户，角色为超级管理员
- admin用户不能修改和删除，只能改密码
- 角色信息关联显示在用户列表中

**细节考虑**:
- 遵循前置任务开发成果及要求，保证开发一致性
- 用户角色分为超级管理员、普通用户等

---

### 1.3 功能管理

**功能**: 将菜单侧实现功能化，动态管理所有功能模块

**实现流程**:
1. 动态管理所有功能模块
2. 支持新增、修改、删除功能
3. 功能与菜单关联映射

**技术考虑**:
- 功能模块动态配置，无需硬编码
- 支持权限绑定

**细节考虑**:
- 遵循前置任务开发成果及要求

---

### 1.4 权限管理

**功能**: 功能与角色的映射关系模块

**实现流程**:
1. 允许新增、修改、删除权限
2. 采用二级联动方式实现权限配置
3. 权限与角色关联

**技术考虑**:
- 二级联动：功能模块→具体权限
- 权限粒度控制到具体操作

**细节考虑**:
- 遵循前置任务开发成果及要求

---

### 1.5 角色管理

**功能**: 管理系统角色，支持角色新增和权限分配

**实现流程**:
1. 默认超级管理员角色（不能修改和删除）
2. 允许新增角色
3. 采用二级联动方式实现角色权限配置
4. 支持修改、删除角色

**技术考虑**:
- 超级管理员角色为系统内置，不可删除
- 二级联动：角色→权限分配

**细节考虑**:
- 遵循前置任务开发成果及要求

---

## 二、模型引擎模块

### 2.1 模型服务管理

**功能**: 动态管理AI模型服务，支持OpenAI-API范式的模型接入

**实现流程**:
1. 以橱窗列表的页面风格展示模型服务
2. 支持动态新增、修改、删除模型服务
3. 支持可视化配置满足OpenAI-API范式的模型接入及接口调用服务
4. 支持模型Token统计（可视化）
5. 支持分页，一行3列，6条数据为一页
6. 支持对新增的模型进行对话测试功能
7. 模型响应SSE（流式响应）
8. 支持设置模型为系统默认模型（全局优先调用）

**技术考虑**:
- 页面风格炫酷、高级感强（因涉及AI大模型）
- 支持OpenAI-API标准接口格式
- Token统计可视化展示
- SSE流式响应支持

**细节考虑**:
- 模型服务代码示例：
  ```
  curl https://aigc-api.aitoolcore.com/api/v1/chat/completions \
    -H "Authorization: Bearer YOUR_API_KEY" \
    -H "Content-Type: application/json" \
    -d '{
      "model": "deepseek-v3",
      "messages": [
        {"role": "user", "content": "你好"}
      ],
      "stream": false
    }'
  ```
- 默认模型全局优先调用

---

## 三、瞭望采集模块

### 3.1 瞭望数据源管理

**功能**: 动态可视化规则配置，支持新增、删除、修改采集源的规则

**实现流程**:
1. 支持新增采集源规则
2. 支持删除、修改采集源规则
3. 动态可视化配置采集参数
4. 支持批量采集功能

**技术考虑**:
- 采集源动态管理
- 请求伪造技术应用（CSRF、SSRF防护）
- 支持URL参数动态替换（如{关键字}、{分页步进}）

**细节考虑**:
- 百度新闻采集源示例：
  - 采集入口URL：https://www.baidu.com/s?tn=news&word={关键字}
  - 分页参数：pn={分页步进，默认0=第一页，10=第二页，20=第三页...}
  - 需要配置完整的Request Header

---

### 3.2 瞭望采集界面

**功能**: 开发类似搜索引擎的采集页面，支持动态选择采集源

**实现流程**:
1. 开发独立设计风格的采集页面（不与Layui风格同步）
2. 输入框下方提供采集源的动态选择功能（开关样式）
3. 采集源选择面板下方提供参数配置：一次有效采集数量和页数
4. 参数与URL中的参数同步
5. 采集到的数据保存到SQLite数据库中

**技术考虑**:
- 界面要求炫酷、好看
- 用户交互体验简单
- 独立设计风格，不与Layui同步

**细节考虑**:
- 采集源选择采用开关样式
- 参数配置与URL参数同步

---

## 四、数据仓库模块

### 4.1 数据仓库列表

**功能**: 以列表显示通过瞭望采集到的数据

**实现流程**:
1. 列表显示采集数据，一页20条
2. 支持删除、批量删除
3. 支持AI深度采集（单条或多条选择）
4. AI处理列标注深度采集状态

**技术考虑**:
- 分页查询（每页20条）
- 批量操作支持
- AI深度采集状态标记

**细节考虑**:
- 如果没有点击进行深度采集，标注为"无"
- 如果进行了深度采集，标注为"是"

---

### 4.2 AI深度采集

**功能**: 对数据仓库记录中的采集数据源进行深度解析，获得详细内容

**实现流程**:
1. 支持单条或多条数据选择进行深度采集
2. 通过技术手段深度解析数据源
3. 获得详细内容并保存到数据详细表中
4. 详细内容与原数据进行关联
5. 深度采集过程中有进度提示
6. 有采集日志提示
7. 对结果有简单的统计分析
8. 深度采集完成后在数据仓库记录中标注状态

**技术考虑**:
- 技术栈：模型引擎中的默认大模型服务 + claw4ai
- 进度提示和日志提示
- 结果统计分析

**细节考虑**:
- 详细内容保存到数据详细表
- 与原数据关联
- AI处理列标注"是"，AI列标注深度采集状态

---

## 五、接口管理模块

### 5.1 接口URL管理

**功能**: 管理互联网上搜集到的API，为系统其他模块提供API接口服务

**实现流程**:
1. 支持新增接口（接口名称、接口地址、请求方式、返回格式、请求示例）
2. 支持删除、修改接口
3. 支持分页显示
4. 请求方式和返回格式改成下拉选择形式
5. QPS限制：x秒内最多x次

**技术考虑**:
- 接口名称、接口地址、请求方式、返回格式、请求示例显示在一行
- 请求方式：GET/POST/PUT/DELETE等下拉选择
- 返回格式：JSON/XML等下拉选择
- QPS限制可配置

**细节考虑**:
- 删除"编码"字段
- 不需要"参数说明"字段
- QPS限制改为"x秒内最多x次"

---

## 六、数字员工模块

### 6.1 数字员工管理（后台）

**功能**: 基于大模型服务+接口管理中的API服务完成综合业务管理

**实现流程**:
1. 支持新增、修改、删除数字员工
2. 标明数字员工的分类（AI/普通）
3. 需要有说明
4. 兼容两类员工的参数及配置
5. 别名动态管理和可配置

**技术考虑**:
- AI类数字员工：通过模型引擎中的默认模型+提示词（prompt）实现智能对话
- 普通类数字员工：通过接口管理中的API服务提供响应数据
- 别名配置后，前台同步用新的别名才能与该数字员工进行对话

**细节考虑**:
- @川小农：AI对话类，通过模型+prompt实现
- @天气：API接口类，通过天气API提供数据
- @音乐：API接口类，通过音乐API提供数据
- 模型根据提示词及用户的问题，响应（SSE）结果

---

### 6.2 数字员工系统提示词

**功能**: 为每个数字员工定义人格、专业领域和回复规范

**实现流程**:
1. 用户创建数字员工时配置prompt_template模板
2. 模板中使用{query}或{user_query}作为用户输入占位符
3. 用户提问时，系统替换模板变量后作为system prompt发送给LLM
4. 如果未配置模板，使用默认提示词："你是数字员工「{assistant_name}」，请用专业友好的语气回答用户问题"

**技术考虑**:
- 支持两级模板：Worker.system_prompt优先级高于Job.prompt_template
- 变量替换支持{query}和{{query}}两种写法（避免模板引擎冲突）
- 数字员工分为三种类型：AI（调用大模型）、本地（本地脚本）、API（外部接口）

**细节考虑**:
- 每个数字员工关联一个Job（工作），Job定义工具集合和基础提示词
- Worker可继承Job的prompt_template，也可自定义覆盖
- 支持Function Calling（工具调用），从job.tool_names加载工具schema

---

### 6.3 天气助手

**功能**: 专业天气查询助手，通过调用天气工具获取实时数据后以自然语言播报

**实现流程**:
1. 分析用户输入，提取纯城市名（去掉"天气"、"今天"、"怎么样"等修饰词）
2. 调用get_weather(city_name)工具获取实时天气数据
3. 根据返回的结构化数据生成自然语言播报
4. 播报包含：当前温度、体感温度、天气描述、湿度、风速风向、未来预报
5. 回复控制在200字以内，适当使用emoji增强可读性

**技术考虑**:
- 城市名提取：当用户输入包含冗余词汇时，用LLM提取纯城市名
- 工具调用：必须通过get_weather工具获取数据，禁止自行编造天气数据
- 播报生成：使用低温(0.3)确保播报内容稳定一致
- 失败处理：工具调用失败时告知用户暂时无法获取信息

**细节考虑**:
- 群聊场景中天气助手跳过历史上下文（防止|effect:格式标记污染LLM输出）
- 检测LLM回复中的天气特效关键字（sunny/cloudy/rainy/snowy/stormy/foggy）触发前端动画
- 支持城市名变体尝试（如中文名失败后尝试英文名）

---

## 七、智能问数模块

### 7.1 用户侧登录注册

**功能**: 前端用户登录注册，与管理用户共用一套鉴权逻辑

**实现流程**:
1. 利用后端登录模块实现前端用户登录
2. 区分角色，前端只有普通用户角色
3. 允许用户注册成为普通用户
4. 预留会员角色（订阅模式），本期不开发

**技术考虑**:
- 共用一套鉴权逻辑
- 角色区分：管理员/普通用户

**细节考虑**:
- 前端只有普通用户角色
- 会员角色预留

---

### 7.2 智能问数界面

**功能**: 设计并实现类似豆包/ChatGPT风格的AI对话界面

**实现流程**:
1. 左侧为任务历史记录，右侧为对话工作区
2. 通过调用后台模型引擎中的默认模型实现与AI对话
3. 默认AI对话需要意图识别：哪些是普通问题，哪些是需要通过与SQLite对话的问题
4. 智能问数的核心：通过AI大模型与数据库中的通过采集和深度采集的数据进行对话
5. 通过@xxx实现与后台数字员工对话的功能
6. 响应数据采用流式响应
7. 采用Markdown渲染数据响应结果
8. 左侧实现模型服务切换（对应后台模型引擎中的模型服务）
9. 左侧记录历史对话的任务，点击可以查看和回放历史对话数据
10. 右侧采用ChatGPT/豆包的对话风格
11. 对话输入框支持普通输入+@数字员工输入

**技术考虑**:
- 意图识别：普通问题 vs 数据库查询问题
- AI大模型通过类似call function等方式实现功能
- SSE流式响应
- Markdown渲染

**细节考虑**:
- 历史对话可回放
- 模型服务可切换
- 支持@数字员工输入

---

### 7.3 SQL查询生成

**功能**: 用户通过自然语言提问，系统调用大语言模型生成SQL查询并返回数据结果

**实现流程**:
1. 接收用户自然语言问题
2. 动态注入当前用户可见的数据库Schema信息（表结构、字段说明）
3. 提供最新3条数据样本作为参考上下文
4. 调用LLM生成SQL查询语句
5. 执行SQL并返回结果（限制只能执行SELECT查询）
6. 采用SSE流式响应，前端实现打字机效果

**技术考虑**:
- 数据范围控制：系统提示词中动态注入当前用户权限范围内的数据集描述（普通用户仅可查询公开数据，管理员可查询全部采集数据）
- 安全控制：限制只能使用SELECT语句，防止SQL注入
- 时间处理：create_at字段存储的是UTC时间，需在提示词中说明
- 输出格式：要求返回JSON格式包含sql、explanation、result_mode三个字段

**细节考虑**:
- 每个会话（session_id）对应一系列问答，存储在im_messages表中，type字段标记为qa
- 支持多轮对话，后台自动加载最近10轮对话作为上下文
- 用户可继续之前会话，历史记录持久化保存

---

## 八、智能聊天模块

### 8.1 聊天系统架构

**功能**: 基于现有系统新增智能聊天子系统，实现类似微信的即时通信功能

**实现流程**:
1. 在现有系统侧边栏菜单中增加一级菜单项"智能聊天"
2. 所有已登录用户均可访问，无需额外授权
3. 从未使用过聊天功能时显示空状态提示
4. 建立WebSocket长连接层
5. 消息收发、存储、拉取历史消息

**技术考虑**:
- 沿用现有系统的MVC模式
- 针对聊天特性增加WebSocket长连接层
- Tornado Web服务器处理HTTP请求和WebSocket连接
- 心跳机制：客户端每隔30秒发送ping帧，服务器回复pong

**细节考虑**:
- 图标采用消息气泡或对话风格
- 空状态提示："暂无会话，去添加好友开始聊天"

---

### 8.2 聊天主界面

**功能**: 左右结构的聊天主界面，支持会话列表和聊天窗口

**实现流程**:
1. 左侧面板（会话/联系人列表）：
   - 顶部标签页：支持切换"会话"和"通讯录"
   - 会话列表：展示最近聊天的私聊和群聊记录，按最后消息时间倒序排列
   - 通讯录列表：展示好友列表和群聊列表，支持分组
   - 搜索框：全局搜索好友昵称、群名称，支持模糊匹配
2. 右侧面板（聊天窗口）：
   - 顶部：显示当前聊天对象、成员数（群聊）等
   - 中间区域：消息展示区，按时间顺序展示历史消息
   - 底部输入区：文本输入框、表情选择按钮、文件上传按钮、发送按钮

**技术考虑**:
- 左侧面板宽度280px（可折叠或拖拽调整）
- 右侧聊天窗口自适应
- 响应式：窗口宽度小于900px时，左侧面板自动隐藏

**细节考虑**:
- 会话列表项显示：头像、名称、最后消息预览、时间、未读红点
- 通讯录列表：分组标题可折叠，好友项显示在线状态
- 消息气泡：自己发送的靠右，对方消息靠左
- 支持表情、文件、@提及功能

---

### 8.3 实时聊天系统

**功能**: 基于WebSocket的实时聊天系统，支持私聊、群聊、数字员工自动回复

**实现流程**:
1. 用户建立WebSocket连接，发送认证token
2. 服务端验证token，获取用户信息
3. 用户发送消息，服务端根据消息类型路由：
   - 私聊消息：直接转发给目标用户
   - 群聊消息：转发给群内所有成员
   - @数字员工：触发自动回复逻辑
4. 消息持久化存储到im_messages表
5. 支持消息撤回、已读回执、在线状态

**技术考虑**:
- WebSocket连接管理：使用字典存储user_id到websocket的映射
- 心跳机制：定期发送ping/pong保持连接活跃
- 消息格式：JSON格式包含type、content、sender、timestamp等字段
- 离线消息：用户上线时推送未读消息
- 群聊成员管理：支持加入、退出、踢出操作

**细节考虑**:
- 群聊@数字员工：解析消息中的@mentions，找出被@的用户ID
- 查询被@用户中哪些是启用的数字员工（Worker.user_id）
- 对每个被@的数字员工获取该群中最近100条相关消息作为上下文
- 构建LLM消息上下文（按时间正序），调用LLM生成回复
- 天气助手特殊处理：跳过历史上下文，只传入本次用户提问
- 检测LLM回复中的天气特效关键字触发前端动画

---

### 8.4 状态与反馈

**功能**: WebSocket连接状态、消息送达状态、未读消息提醒

**实现流程**:
1. WebSocket连接状态：在界面右下角显示连接状态（在线/离线/重连中）
2. 消息送达状态：发送中→已发送→失败（红色感叹号）
3. 未读消息提醒：浏览器标签页标题闪烁或显示未读数量

**技术考虑**:
- WebSocket断开后自动重连（指数退避）
- 重连成功后自动拉取离线消息
- 使用Web Notifications API可选

**细节考虑**:
- 消息发送后本地先显示"发送中"占位
- 服务器确认后变为"已发送"
- 失败则显示红色感叹号并支持重发

---

### 8.5 页面布局与优化

**功能**: 深色科技感风格的聊天界面优化

**实现流程**:
1. 整体布局：聊天主界面占满内容区域
2. 左右比例：左侧面板宽度280px，右侧自适应
3. 响应式：窗口宽度小于900px时左侧自动隐藏
4. 虚拟滚动：消息列表只渲染可视区域附近的DOM
5. 图片懒加载：聊天中的图片消息使用缩略图
6. 本地缓存：已加载的会话列表、历史消息存入IndexedDB

**技术考虑**:
- 沿用现有系统的深色背景（#050810渐变）、发光边框、粒子动效
- 聊天窗口内的滚动条使用细窄自定义样式
- 按钮hover效果：渐变光晕、轻微上浮

**细节考虑**:
- 会话列表项：头像圆形或圆角矩形，名称最多两行
- 最后消息预览一行，超出用"..."结尾
- 时间显示：时/分/秒，昨天的显示"昨天"，更早显示"月/日"
- 未读红点：右上角显示数字（超过99显示"99+"）

---

## 九、数据清洗模块

### 9.1 AI数据清洗系统

**功能**: 从原始HTML内容中提取结构化信息（标题、正文、域名、摘要）

**实现流程**:
1. 接收原始采集的HTML内容
2. 调用LLM进行AI清洗，提取结构化字段
3. 验证返回的JSON格式和字段完整性
4. 清洗成功后自动写入WarehouseArticle表
5. 记录清洗日志（成功/失败状态）

**技术考虑**:
- 系统提示词要求提取四个字段：title、content、domain_name、summary
- title从HTML的title标签或首段中提取
- content需去除script、style标签和所有HTML标签
- domain_name从URL中提取，去掉"www."前缀
- summary为200字以内的内容摘要
- 强制JSON格式返回，不支持markdown代码块标记

**细节考虑**:
- 如果数据无法解析，返回{"error": "原因说明"}
- 温度参数设为0.3确保精确提取
- 支持正则规则清洗作为回退方案（不消耗Token）
- 清洗失败标记为failed状态，支持重试

---

## 十、采集日志模块

### 10.1 采集日志记录

**功能**: 记录每次采集任务的执行过程、成功/失败状态、耗时统计

**实现流程**:
1. 采集任务开始前创建CrawlLog记录（task_id、source_id、source_name、keyword）
2. 采集过程中实时更新日志状态（running/success/error）
3. 记录每页采集结果（成功条数、失败原因）
4. 采集完成后更新日志（total_count、saved_count、elapsed_time）
5. 采集失败时记录错误信息（截取前500字符）

**技术考虑**:
- 日志状态：running（执行中）、success（成功）、error（失败）
- 关联outlook_tasks表，使日志数据能在数据仓库中展示
- 支持实时状态回调（status_callback），前端可实时查看采集进度
- 错误信息截取限制500字符，防止日志过长

**细节考虑**:
- 定时任务执行失败时自动更新crawl_schedules表的is_enabled状态
- 每日任务（daily）执行失败后保持启用，一次性任务（once）执行失败后禁用
- 日志记录包含时间戳、类型（info/warn/success/error）、消息内容

---

## 十一、定时采集模块

### 11.1 定时任务调度

**功能**: 基于APScheduler实现定时自动采集，支持每日定时和一次性任务

**实现流程**:
1. 用户在crawl_schedules表中配置定时任务（cron表达式、任务类型、数据源）
2. 应用启动时调用load_schedules()加载所有启用的定时任务
3. 根据schedule_type创建不同的触发器：
   - daily：每日定时，使用CronTrigger(hour=小时, minute=分钟)
   - once：一次性任务，使用DateTrigger(run_date=具体时间)
   - cron：自定义cron表达式，解析为CronTrigger参数
4. 任务触发时调用execute_crawl_task()执行采集
5. 采集完成后更新last_run时间，一次性任务自动禁用

**技术考虑**:
- 时区处理：使用北京时间（UTC+8），所有时间计算基于BEIJING_TZ
- 任务去重：启动时先移除所有旧crawl_前缀的job，再重新加载
- 容错机制：misfire_grace_time=3600（每日任务）/ 60（一次性任务）
- 任务合并：coalesce=True，错过多次执行时只执行一次

**细节考虑**:
- 一次性任务执行后自动设置is_enabled=0，防止重复执行
- 任务时间已过（run_date <= now）时自动禁用并记录日志
- 采集任务创建outlook_tasks记录，使数据能在数据仓库中展示
- 工作流引擎每分钟检查一次（workflow_engine_check），自动执行定时工作流

---

## 十二、深度采集模块

### 12.1 AI深度解析

**功能**: 对采集的网页内容进行深度解析和信息提取

**实现流程**:
1. 采集完成后获取页面正文（clean_text）
2. 调用LLM进行深度解析，提取核心内容和关键要点
3. 去除明显的导航、广告、版权声明等无关信息
4. 用200字以内概括核心内容
5. 提取3-5个关键要点
6. 返回JSON格式：summary、key_points、content

**技术考虑**:
- 系统提示词："你是一个信息整理助手。请根据以下内容，完成提取和整理工作"
- 输入包含：文章标题、搜索摘要、页面正文
- 要求尽可能多地保留原文内容，不要过度删减
- 输出格式：{"summary": "核心内容概括", "key_points": "要点1；要点2；要点3", "content": "完整正文"}

**细节考虑**:
- 如果没有明显无关内容，完整保留原文
- 关键要点用分号分隔
- 深度解析结果存储在outlook_data表的ai_deep_status字段标记处理状态
- 数智大屏展示深度处理统计数据

---

## 十三、智慧舆情模块

### 13.1 舆情分析系统

**功能**: AI自动分析多源舆情数据，生成风险评估报告

**实现流程**:
1. 从两个数据源采集增量数据：
   - 智能问数（AI对话）：用户对话记录，反映关注热点和情绪倾向
   - 数据仓库（清洗后文章）：从数字瞭望采集、经清洗入库的已发布文章
2. 获取上次分析时间，仅分析增量数据
3. 调用LLM进行舆情分析，返回结构化JSON
4. 解析结果并存储到SentimentAnalysis、SentimentEvent、SentimentWord、SentimentLocation表
5. 生成大屏快照（SentimentDashboard）便于快速查询
6. 前端展示词云、事件列表、地点标记、情感分布

**技术考虑**:
- 系统提示词要求分析四个维度：整体情感倾向、事件列表、关键词权重、地理位置
- 整体情感：positive/negative/neutral/mixed
- 事件需包含：事件名称、情感类型、描述、关键词、热度值（0-100）
- 关键词需包含：词、权重（0-100）、情感类型、分类（event/topic/entity/emotion）
- 地点需包含：地点名、描述（后续可解析经纬度用于3D地球标记）
- 强制JSON格式返回，不支持markdown代码块标记

**细节考虑**:
- 分析摘要需区分说明数据来源（智能问数/数据仓库）
- 支持增量分析，避免重复分析历史数据
- 舆情分析结果缓存，避免重复调用LLM
- 词云数据取最近一次舆情分析的TOP50关键词
- 3D地球标记数据从SentimentLocation表获取经纬度和热度值

---

## 十四、数智大屏模块

### 14.1 数据可视化大屏

**功能**: 实时展示系统各项指标和数据趋势，支持手势交互

**实现流程**:
1. 前端加载时调用/api/dashboard/stats获取实时指标
2. 展示核心指标：数据源数量、今日采集量、总采集量、Token消耗、用户数、文章数
3. 调用/api/dashboard/collect-trend获取采集趋势（按天统计）
4. 调用/api/dashboard/token-trend获取Token消耗趋势
5. 调用/api/dashboard/earth获取3D地球标记数据（舆情地点）
6. 调用/api/dashboard/wordcloud获取词云数据
7. 调用/api/dashboard/sentiment-stats获取舆情统计概览
8. 支持手势识别切换页面（食指上/下翻页，握拳暂停/恢复）

**技术考虑**:
- 使用ECharts进行数据可视化（折线图、柱状图、词云、3D地球）
- 采集趋势：按天统计WatchData表的记录数，支持自定义天数（默认7天）
- Token趋势：按天统计TokenStat表的tokens_used总和
- 3D地球：从SentimentLocation表获取经纬度和热度值，标记舆情事件地点
- 词云：从SentimentWord表获取关键词和权重，按sentiment_type着色

**细节考虑**:
- 前端定时刷新（每30秒），保持数据实时性
- 手势识别使用MediaPipe 21个手部关键点，5帧历史缓冲区+3帧共识机制
- 语音播报：使用Web Speech API（SpeechSynthesis），数据更新时语音提示
- 最近数据展示：展示最近3天内采集的20条数据标题
- 大屏支持自动轮播，握拳手势可暂停/恢复

---

## 十五、语音播报模块

### 15.1 语音播报系统

**功能**: 使用浏览器Web Speech API实现文本转语音播报

**实现流程**:
1. 检测浏览器是否支持window.speechSynthesis
2. 创建SpeechSynthesisUtterance对象，设置文本内容
3. 配置语音参数：lang='zh-CN'、rate=1.0（语速）、pitch=1.0（音调）、volume=1.0（音量）
4. 调用window.speechSynthesis.speak()开始播报
5. 支持播报完成回调，触发后续动作

**技术考虑**:
- 技术栈：Web Speech API（SpeechSynthesis），浏览器原生支持
- 语言设置：zh-CN（中文普通话）
- 语速控制：rate参数0.1-10.0，默认1.0正常语速
- 音调控制：pitch参数0.0-2.0，默认1.0正常音调
- 音量控制：volume参数0.0-1.0，默认1.0最大音量

**细节考虑**:
- 手势识别成功时语音提示（如"识别到食指向上手势"）
- 数据更新时语音播报（如"今日新增数据XX条"）
- 舆情分析完成时语音提示
- 播报前检查浏览器兼容性，不支持时静默跳过
- 支持播报队列管理，避免多个播报同时播放

---

## 十六、自动工作流模块

### 16.1 工作流编排

**功能**: 自动化执行"采集→清洗入库→舆情分析"全流程

**实现流程**:
1. 用户创建工作流，配置名称、描述、步骤列表、cron表达式
2. 步骤类型包括：collect（采集）、cleaning（清洗）、sentiment（舆情分析）
3. 每个步骤配置source_ids（数据源ID列表）
4. 定时触发或手动触发工作流执行
5. 后台异步执行（asyncio.create_task），不阻塞主线程
6. 每步记录WorkflowTaskLog（状态、目标、结果、错误信息）
7. 支持任务取消和删除
8. 失败时立即终止并记录错误

**技术考虑**:
- 步骤1-数据采集：遍历所有source_ids，根据配置的URL、Method、Headers、Body发起HTTP请求，保存原始响应到WatchData表
- 步骤2-清洗入库：调用AI清洗接口，对采集数据进行清洗，成功后写入WarehouseArticle表
- 步骤3-舆情分析：从数据仓库和智能问数采集增量数据，进行AI舆情分析
- 工作流状态：pending（待执行）、running（执行中）、completed（完成）、failed（失败）、cancelled（取消）
- 执行日志：记录每个步骤的开始时间、结束时间、状态、结果

**细节考虑**:
- 工作流引擎每分钟检查一次（workflow_engine_check），自动执行到达cron时间的工作流
- 支持手动触发执行，无需等待cron时间
- 工作流执行期间可取消，取消后记录取消状态
- 工作流删除时同时删除关联的执行日志
- 失败步骤记录详细错误信息，便于排查问题

---

## 十七、群聊数字员工模块

### 17.1 群聊@触发逻辑

**功能**: 在群聊中识别@数字员工的消息并自动回复

**实现流程**:
1. 解析消息中的@mentions，找出被@的用户ID
2. 查询这些用户中哪些是启用的数字员工（Worker.user_id）
3. 对每个被@的数字员工：
   - 获取该群中最近100条相关消息（包括用户@该员工的消息 + 该员工的回复）
   - 构建LLM消息上下文（按时间正序）
   - 调用LLM生成回复
   - 将回复作为群聊消息发送
4. 系统提示词构建：worker.system_prompt或job.prompt_template
5. 替换模板中的{user_query}变量

**技术考虑**:
- 天气助手特殊处理：跳过所有历史上下文，只传入本次用户提问
- 其他数字员工：保留历史上下文，按时间顺序添加上下文消息
- 支持Function Calling（工具调用），从job.tool_names加载工具schema
- 检测LLM回复中的天气特效关键字（sunny/cloudy/rainy/snowy/stormy/foggy）

**细节考虑**:
- 天气助手跳过历史上下文是为了防止|effect:格式标记污染LLM输出
- 消息上下文按时间正序排列，确保LLM理解对话顺序
- 数字员工回复作为群聊消息发送，所有群成员可见
- 支持多个数字员工同时被@，分别生成回复

---

## 十八、提示词模板管理机制

### 18.1 数据库存储

**功能**: 管理数字员工和工作（Job）的提示词模板

**实现流程**:
1. 数字员工提示词存储在assistants表的prompt_template字段
2. 工作提示词存储在jobs表的prompt_template字段
3. 支持变量替换：{query}、{user_query}、{{query}}
4. Worker可继承Job的prompt_template，也可自定义覆盖
5. Worker.system_prompt优先级高于Job.prompt_template

**技术考虑**:
- assistants表关键字段：prompt_template、category（AI/本地/API）、model_id
- jobs表关键字段：prompt_template、code（唯一编码）、tool_names（工具名称列表）
- 变量替换：使用字符串replace方法，支持{query}和{{query}}两种写法
- 默认提示词：如果未配置模板，使用"你是数字员工「{assistant_name}」，请用专业友好的语气回答用户问题"

**细节考虑**:
- 工作编码（code）用于标识特殊工作类型（如weather_assistant、data_cleaner、sentiment_analysis）
- 工具名称列表（tool_names）为JSON数组，定义该工作可调用的工具
- 提示词模板支持富文本格式（Markdown、emoji等）
- 模板变更实时生效，无需重启服务

---

## 十九、提示词设计最佳实践

### 19.1 通用原则

1. **角色定义清晰**: 明确AI的身份和专业领域
2. **任务描述具体**: 详细说明需要完成的任务
3. **输出格式规范**: 指定返回格式（JSON/文本/Markdown）
4. **约束条件明确**: 列出禁止事项和限制条件
5. **示例提供参考**: 给出输入输出示例增强理解

### 19.2 安全控制

1. **SQL注入防护**: 限制只能执行SELECT查询
2. **Token限制**: 根据任务类型设置合理的max_tokens
3. **温度控制**: 确定性任务用低温(0.3)，创意任务用高温(0.7)
4. **数据范围控制**: 动态注入用户可见的数据集描述

### 19.3 性能优化

1. **流式响应**: 使用SSE实现打字机效果，提升用户体验
2. **上下文管理**: 加载最近10轮对话作为上下文
3. **缓存机制**: 舆情分析结果缓存，避免重复调用
4. **降级策略**: AI调用失败时提供fallback方案

---

## 二十、提示词调试经验

### 20.1 常见问题

1. **输出格式不稳定**: 增加格式示例和约束说明
2. **中文理解偏差**: 使用中文编写提示词，避免中英混杂
3. **上下文丢失**: 控制上下文长度，避免超出模型窗口限制
4. **响应过长**: 设置max_tokens限制，要求简洁回复

### 20.2 调试技巧

1. **降低temperature**: 提高输出稳定性和可重复性
2. **增加示例**: 提供few-shot示例增强理解
3. **分步执行**: 复杂任务拆分为多个简单提示词
4. **日志记录**: 记录完整提示词和响应，便于排查问题

---

## 附录：提示词文件索引

| 模块 | 提示词类型 | 文件路径 |
|------|-----------|---------|
| 智能问数 | SQL查询生成 | `app/controllers/chat.py` |
| 数字员工 | 默认系统消息 | `app/controllers/chat.py`、`app/controllers/im_ws.py` |
| 天气助手 | 系统提示词 | `CToS/CToS2Back/external.py` |
| 天气助手 | 播报生成 | `CToS/CToS2Back/external.py` |
| 天气助手 | 城市提取 | `CToS/CToS2Back/external.py` |
| 瞭望采集 | 关键词扩展 | `app/models/outlook.py` |
| 瞭望采集 | AI数据清洗 | `app/models/outlook.py` |
| 数据清洗 | AI清洗系统 | `CToS/CToS2Back/services/cleaning_service.py` |
| 智慧舆情 | 舆情分析 | `CToS/CToS2Back/services/sentiment_service.py` |
| 深度采集 | AI深度解析 | `app/models/outlook.py` |
| 群聊数字员工 | 系统提示词构建 | `CToS/CToS2Back/routers/im.py` |
| 模型引擎 | 通用调用 | `app/models/model.py` |

---

**文档维护说明**:
- 本文档仅包含设计实现的核心提示词
- 不包含开发过程中的中间迭代版本
- 新增提示词请及时更新本文档
- 提示词变更需同步更新对应的代码文件
