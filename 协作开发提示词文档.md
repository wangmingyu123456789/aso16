
## 一、用户侧认证模块

### 1.1 用户注册功能

**功能**: 实现用户账号注册，包含表单校验、图形验证码、唯一性校验

**实现流程**:
1. 用户填写注册表单（用户名、邮箱、密码、确认密码）
2. 前端表单校验（必填项、密码强度、邮箱格式）
3. 基础图形验证码校验（防止机器批量注册）
4. 后端校验用户名/邮箱唯一性
5. 密码使用PBKDF2-HMAC-SHA256加密后存储
6. 注册成功后跳转登录页

**技术考虑**:
- 前端组件全部使用本地Layui/Bootstrap/FontAwesome，禁止CDN
- 沿用现有PBKDF2-HMAC-SHA256密码加密规则，保持加密一致性
- 图形验证码使用本地生成（如Pillow库生成图片验证码）
- 用户名/邮箱唯一性校验通过数据库查询实现

**细节考虑**:
- 密码强度要求：至少8位，包含字母和数字
- 验证码有效期限制（如5分钟）
- 注册失败时保留用户已填写内容
- 遵循项目MVC架构规范，新增模块独立分层

---

### 1.2 密码找回功能

**功能**: 接入邮件SMTP配置，实现邮箱验证码找回密码、重置密码流程

**实现流程**:
1. 用户点击"忘记密码"，进入密码找回页面
2. 输入注册邮箱，系统发送验证码邮件
3. 用户输入收到的验证码
4. 验证码校验通过后，进入重置密码页面
5. 输入新密码和确认密码
6. 密码更新成功，跳转登录页

**技术考虑**:
- SMTP配置存储在系统配置表中（支持后台配置）
- 验证码生成后存储到缓存/数据库，设置过期时间（如10分钟）
- 邮件发送使用异步任务，避免阻塞主线程
- 验证码错误次数限制（如5次错误后锁定）

**细节考虑**:
- 邮件模板包含验证码、有效期说明、系统名称
- 验证码为6位数字
- 同一邮箱发送验证码频率限制（如60秒内只能发送一次）
- 密码重置链接/验证码一次性使用，使用后失效

---

### 1.3 登录记住我功能

**功能**: 延长Secure Cookie会话有效期，支持7天免登录

**实现流程**:
1. 登录页面增加"记住我"复选框
2. 用户勾选后，设置Cookie有效期为7天
3. 未勾选时，使用默认会话有效期（如浏览器关闭失效）
4. 用户再次访问时，自动读取Cookie完成登录

**技术考虑**:
- Cookie新增Secure/HttpOnly/SameSite属性，防会话劫持
- Secure：仅HTTPS传输
- HttpOnly：禁止JavaScript读取，防XSS攻击
- SameSite：设置为Strict或Lax，防CSRF攻击
- 记住我Token使用加密签名，防止伪造

**细节考虑**:
- 记住我Token与用户ID绑定
- 用户主动退出登录时清除记住我Cookie
- 密码修改后失效所有记住我Token
- 前端UI提示用户"记住我"功能的安全风险

---

### 1.4 前台登录页UI

**功能**: 采用简约科技风，与后台登录风格区分，适配响应式布局

**实现流程**:
1. 设计独立的前台登录页面
2. 采用简约科技风格（与后台企业化风格区分）
3. 适配响应式布局（PC/平板/手机）
4. 集成用户名、密码、验证码、记住我、忘记密码等元素

**技术考虑**:
- 前端组件使用本地Layui/Bootstrap/FontAwesome
- 响应式断点：≥1200px（PC）、768-1199px（平板）、<768px（手机）
- 背景可采用科技感渐变或粒子动效
- 表单居中布局，最大宽度限制

**细节考虑**:
- 与后台登录页风格明显区分
- 登录失败时显示友好错误提示
- 加载状态显示（登录中...）
- 支持回车键提交

---

### 1.5 会话安全完善

**功能**: Cookie安全属性配置，防会话劫持

**实现流程**:
1. 所有Cookie设置Secure属性
2. 所有Cookie设置HttpOnly属性
3. 设置SameSite属性为Strict或Lax
4. 会话Token使用加密签名
5. 会话超时自动失效

**技术考虑**:
- Secure：仅HTTPS环境下生效
- HttpOnly：防止JavaScript访问Cookie
- SameSite：防止跨站请求伪造
- 会话Token包含用户ID、过期时间、签名

**细节考虑**:
- 开发环境（HTTP）可临时关闭Secure属性
- 会话超时时间可配置
- 异常登录检测（如IP变化、设备变化）
- 日志记录会话创建和销毁事件

---

## 二、瞭望管理采集能力增强

### 2.1 瞭望数据源规则模板化

**功能**: 将百度新闻采集源封装为通用模板，支持后续一键新增其他采集源

**实现流程**:
1. 分析百度新闻采集源结构，提取通用字段
2. 设计采集源模板结构（URL模板、Header模板、分页规则、解析规则）
3. 后台支持基于模板快速新增采集源
4. 模板支持变量替换（如{关键字}、{分页步进}）

**技术考虑**:
- 模板结构包含：名称、URL模板、Method、Headers、分页参数、解析规则
- URL模板支持变量占位符
- Headers支持动态配置
- 解析规则支持XPath/CSS选择器配置

**细节考虑**:
- 百度新闻采集源作为默认模板
- 新增采集源时只需填写差异化配置
- 模板版本管理，支持模板更新
- 采集源启用/禁用状态管理

---

### 2.2 采集请求头可视化配置

**功能**: 后台新增Header表单配置项，支持自定义Cookie、User-Agent、Referer等参数动态录入

**实现流程**:
1. 采集源编辑页面新增Header配置区域
2. 支持动态添加/删除Header键值对
3. 常用Header提供快捷填入（User-Agent、Cookie、Referer等）
4. Header配置保存到数据库，采集时动态加载

**技术考虑**:
- Header配置以JSON格式存储
- 前端支持动态表单（点击"+"添加新行）
- Header键值对校验（键不能为空）
- 采集请求时合并默认Header和自定义Header

**细节考虑**:
- 提供常用User-Agent预设
- Cookie支持多行录入
- Header配置支持导入/导出
- 敏感Header（如Cookie）加密存储

---

### 2.3 采集分页参数自动化适配

**功能**: 联动前端页数、单页采集数量配置，自动拼接URL中word关键字、pn分页步进参数

**实现流程**:
1. 采集界面提供页数和单页数量配置
2. 根据配置自动计算分页参数
3. 自动拼接URL中的word（关键字）和pn（分页步进）参数
4. 支持预览最终请求URL

**技术考虑**:
- 分页步进规则可配置（如百度新闻：0、10、20...）
- URL模板解析，自动替换变量
- 分页参数与采集源配置联动
- 请求URL预览功能

**细节考虑**:
- 默认页数：1页，单页数量：10条
- 最大页数限制（防止过度采集）
- 分页参数错误时友好提示
- 采集前显示预计请求次数

---

### 2.4 采集任务手动启停

**功能**: 瞭望采集界面新增任务开始/停止按钮，实时展示采集进度条

**实现流程**:
1. 采集界面新增"开始采集"和"停止采集"按钮
2. 点击开始后，后台启动采集任务
3. 前端通过WebSocket或轮询获取采集进度
4. 实时展示进度条（百分比、已采集条数、预计剩余时间）
5. 点击停止后，中断采集任务

**技术考虑**:
- 采集任务异步执行（asyncio.create_task）
- 进度状态存储在内存/缓存中
- 前端定时轮询进度接口（如每秒一次）
- 停止任务通过标志位控制

**细节考虑**:
- 进度条显示：当前页/总页数、成功条数、失败条数
- 任务完成后显示统计结果
- 停止任务时保存已采集数据
- 任务异常时显示错误信息

---

### 2.5 采集数据去重规则

**功能**: 基于标题、链接做重复数据过滤，避免SQLite存储冗余数据

**实现流程**:
1. 采集到新数据时，先查询数据库是否存在相同标题或链接
2. 如果存在，跳过该条数据
3. 如果不存在，插入数据库
4. 记录去重数量

**技术考虑**:
- 去重规则：标题相同或链接相同即视为重复
- 数据库查询使用索引优化（title、url字段建索引）
- 批量采集时先去重再入库
- 去重日志记录

**细节考虑**:
- 标题去重支持模糊匹配（如去除前后空格）
- 链接去重支持URL规范化（去除参数）
- 去重统计显示在采集结果中
- 支持强制入库选项（忽略去重）

---

## 三、数据仓库模块

### 3.1 SQLite数据表结构规范化

**功能**: 统一瞭望采集数据、对话历史、模型调用日志、用户信息表字段设计

**实现流程**:
1. 梳理现有数据表结构
2. 统一字段命名规范（如create_at、update_at）
3. 统一数据类型和长度
4. 添加必要的索引
5. 确保后续可无缝迁移MySQL

**技术考虑**:
- 字段命名：小写下划线风格
- 时间字段：统一使用TEXT类型存储ISO格式时间
- ID字段：统一使用INTEGER PRIMARY KEY AUTOINCREMENT
- 外键关系明确
- 预留MySQL兼容字段类型

**细节考虑**:
- 瞭望采集数据表：outlook_data
- 对话历史表：im_messages
- 模型调用日志表：model_logs
- 用户信息表：users
- 所有表包含create_at、update_at字段

---

### 3.2 数据仓库列表增强

**功能**: 原有20条/页分页基础上，新增按关键字搜索、按采集时间筛选、按数据源筛选

**实现流程**:
1. 数据仓库列表页新增搜索框
2. 新增采集时间筛选（日期范围选择器）
3. 新增数据源筛选（下拉选择）
4. 筛选条件组合查询
5. 支持重置筛选条件

**技术考虑**:
- 关键字搜索：标题、内容、域名模糊匹配（ILIKE）
- 时间筛选：create_at字段范围查询
- 数据源筛选：source_id关联查询
- 分页参数保持（page/page_size）
- 查询结果缓存

**细节考虑**:
- 搜索框支持回车键搜索
- 筛选条件显示在URL参数中（支持分享链接）
- 无结果时显示友好提示
- 筛选条件数量显示

---

### 3.3 批量数据导出

**功能**: 支持选中采集数据导出为Excel/CSV文件

**实现流程**:
1. 数据列表支持多选（复选框）
2. 新增"导出"按钮
3. 选择导出格式（Excel/CSV）
4. 后台生成文件并返回下载
5. 前端触发浏览器下载

**技术考虑**:
- Excel生成使用openpyxl库
- CSV生成使用Python内置csv模块
- 导出字段可配置（标题、内容、域名、时间等）
- 大批量导出时异步处理
- 文件大小限制

**细节考虑**:
- 默认导出选中数据
- 未选中时导出当前页数据
- 导出文件名包含时间戳
- 导出进度提示
- 导出失败时错误提示

---

### 3.4 数据状态标记

**功能**: 新增已读/未读、已采集/待采集状态标识

**实现流程**:
1. 数据表新增status字段
2. 状态枚举：read/unread、collected/pending
3. 列表页显示状态标签
4. 支持批量修改状态
5. 点击数据自动标记为已读

**技术考虑**:
- 状态字段使用TEXT类型
- 状态标签使用不同颜色区分
- 批量更新状态接口
- 状态变更日志

**细节考虑**:
- 默认状态：unread、pending
- 已读数据标记为read
- 采集成功标记为collected
- 状态筛选支持

---

### 3.5 基础向量库预留适配

**功能**: 对接Chroma向量库基础接口，为后续AI语义检索预留扩展入口

**实现流程**:
1. 设计向量库接口抽象层
2. 实现Chroma向量库适配器
3. 数据入库时同步生成向量嵌入
4. 提供语义检索接口
5. 预留开关控制是否启用向量检索

**技术考虑**:
- 向量库接口抽象：embed()、search()、delete()
- Chroma向量库本地部署
- 文本向量化使用模型引擎
- 向量检索与关键词检索结合
- 配置项控制是否启用

**细节考虑**:
- 向量维度与模型匹配
- 向量数据与原文数据关联
- 语义检索结果排序
- 向量库异常时降级到关键词检索

---

## 四、系统日志与统计模块

### 4.1 操作日志

**功能**: 记录管理员登录、增删改配置、用户管理、数据源操作全链路日志

**实现流程**:
1. 设计操作日志表结构
2. 关键操作点记录日志
3. 日志包含：操作人、操作类型、操作对象、操作时间、IP地址
4. 后台提供日志查询页面
5. 支持分页查询、按时间筛选、导出日志文件

**技术考虑**:
- 日志表：operation_logs
- 操作类型枚举：login、create、update、delete、export
- 日志记录使用装饰器或中间件
- 日志异步写入，不影响主流程
- 日志定期清理（如保留90天）

**细节考虑**:
- 敏感操作记录详细信息
- 登录日志记录IP、设备信息
- 日志导出支持CSV格式
- 日志查询支持关键字搜索

---

### 4.2 错误日志

**功能**: 捕获后端接口异常、模型调用异常、采集任务异常并入库保存

**实现流程**:
1. 设计错误日志表结构
2. 全局异常捕获中间件
3. 错误日志包含：错误类型、错误信息、堆栈跟踪、请求URL、时间
4. 后台提供错误日志查询页面
5. 支持按错误类型筛选、按时间筛选

**技术考虑**:
- 错误日志表：error_logs
- 异常捕获使用Tornado异常处理
- 堆栈跟踪截取前1000字符
- 错误日志分级（info、warn、error、critical）
- 错误告警（如连续错误发送邮件）

**细节考虑**:
- 模型调用异常单独记录
- 采集任务异常记录任务ID
- 错误日志支持标记已处理
- 错误统计显示

---

### 4.3 模型调用统计

**功能**: 可视化Token消耗、调用次数、成功率、响应时间折线/柱状图

**实现流程**:
1. 模型调用时记录日志
2. 日志包含：模型名称、Token消耗、成功/失败、响应时间
3. 后台统计页面展示图表
4. 支持时间范围切换（今日/本周/本月）
5. 图表类型：折线图、柱状图

**技术考虑**:
- 统计接口：/admin/api/model-stats
- 图表使用本地ECharts
- 数据按天/小时聚合
- 成功率计算：成功次数/总次数
- 响应时间计算平均值

**细节考虑**:
- Token消耗趋势折线图
- 调用次数柱状图
- 成功率百分比显示
- 响应时间单位（毫秒）
- 数据为空时友好提示

---

### 4.4 瞭望采集统计

**功能**: 按日/按周统计采集任务数量、成功条数、失败条数

**实现流程**:
1. 采集任务完成时记录结果
2. 统计接口按时间维度聚合数据
3. 后台展示采集统计图表
4. 支持按日/按周切换
5. 图表类型：柱状图、折线图

**技术考虑**:
- 统计接口：/admin/api/crawl-stats
- 数据按create_at字段聚合
- 成功/失败状态统计
- 图表使用本地ECharts
- 数据缓存（如5分钟）

**细节考虑**:
- 采集任务数量柱状图
- 成功/失败条数堆叠柱状图
- 成功率趋势折线图
- 时间范围选择器
- 数据为空时提示

---

### 4.5 用户访问统计

**功能**: PV/UV访问量、活跃用户统计，后台图表可视化展示

**实现流程**:
1. 用户访问时记录PV
2. 基于Cookie/Session记录UV
3. 活跃用户定义（如7天内登录）
4. 后台展示访问统计图表
5. 支持时间范围切换

**技术考虑**:
- PV统计：每次页面访问+1
- UV统计：基于唯一标识（Cookie/Session ID）
- 活跃用户：last_login_at在7天内
- 图表使用本地ECharts
- 统计数据定时聚合

**细节考虑**:
- PV/UV趋势折线图
- 活跃用户数柱状图
- 用户增长趋势
- 访问高峰时段
- 数据为空时提示

---

## 五、数字员工模块基础

### 5.1 数字员工管理后台

**功能**: 新增员工列表、新增/启用/禁用/编辑数字员工

**实现流程**:
1. 数字员工列表页
2. 支持新增数字员工（名称、类型、提示词、关联模型/接口）
3. 支持启用/禁用数字员工
4. 支持编辑数字员工配置
5. 支持删除数字员工

**技术考虑**:
- 员工类型：AI（模型）、API（接口）
- AI员工关联模型引擎
- API员工关联接口管理
- 提示词模板配置
- 状态管理：enabled/disabled

**细节考虑**:
- 列表显示：名称、类型、状态、创建时间
- 新增时选择员工类型
- 启用/禁用状态切换
- 删除前确认
- 员工数量统计

---

### 5.2 新闻助手落地

**功能**: 复用瞭望采集数据，实现AI聚合新闻、智能摘要生成

**实现流程**:
1. 新闻助手读取瞭望采集数据
2. AI聚合相关新闻（按主题、时间）
3. 生成智能摘要
4. 用户侧展示新闻列表和摘要
5. 支持新闻详情查看

**技术考虑**:
- 复用数据仓库数据
- AI聚合使用模型引擎
- 摘要生成提示词配置
- 新闻分类标签
- 新闻时效性处理

**细节考虑**:
- 新闻列表按时间倒序
- 摘要长度可配置
- 新闻来源标注
- 相关新闻推荐
- 新闻收藏功能

---

### 5.3 数据助手落地

**功能**: 基于数据仓库数据，支持自然语言数据查询、统计分析问答

**实现流程**:
1. 数据助手接收用户自然语言问题
2. AI解析问题意图
3. 生成SQL查询或统计逻辑
4. 执行查询并返回结果
5. 结果以自然语言回复

**技术考虑**:
- 复用智能问数模块逻辑
- 数据范围控制（用户权限）
- SQL生成安全控制（仅SELECT）
- 结果格式化
- 多轮对话支持

**细节考虑**:
- 问题示例提示
- 查询结果表格展示
- 统计分析图表
- 历史记录保存
- 错误处理提示

---

### 5.4 数字员工对话入口

**功能**: 用户侧新增数字员工选择栏，切换不同助手进行问答

**实现流程**:
1. 用户侧新增数字员工选择栏
2. 显示可用的数字员工列表
3. 点击切换当前对话助手
4. 对话界面适配不同助手
5. 助手切换时清空当前对话

**技术考虑**:
- 选择栏样式与对话界面协调
- 可用员工过滤（enabled状态）
- 切换时保存当前对话历史
- 新助手加载默认提示词
- 助手图标和名称显示

**细节考虑**:
- 选择栏位置：对话界面顶部或侧边
- 当前选中助手高亮
- 助手描述信息
- 切换确认提示
- 无可用助手时提示

---

### 5.5 扩展接口预留

**功能**: 预留天气、音乐、电影助手扩展接口，后续可快速新增

**实现流程**:
1. 设计数字员工扩展接口
2. 定义助手类型枚举
3. 预留配置字段
4. 文档说明扩展方式
5. 测试扩展示例

**技术考虑**:
- 助手类型：AI、API、本地脚本
- API助手配置：接口URL、请求方式、参数映射
- 本地脚本助手：脚本路径、执行参数
- 扩展接口文档
- 扩展示例代码

**细节考虑**:
- 天气助手：天气API接口
- 音乐助手：音乐API接口
- 电影助手：电影API接口
- 扩展配置模板
- 扩展测试用例

---

## 六、数智可视化大屏模块

### 6.1 2D炫酷数据大屏

**功能**: 整合系统用户统计、模型调用统计、瞭望采集数据统计

**实现流程**:
1. 设计大屏页面布局
2. 整合各项统计数据
3. 调用API获取实时数据
4. 图表渲染展示
5. 定时刷新数据

**技术考虑**:
- 大屏独立UI风格，不依赖Layui布局
- 图表使用本地ECharts
- 数据接口：/api/dashboard/stats
- 定时刷新（如30秒）
- 全屏展示支持

**细节考虑**:
- 深色科技风格
- 发光边框效果
- 粒子动效背景
- 数字翻牌动画
- 数据轮播展示

---

### 6.2 通用图表组件

**功能**: 集成柱状图、折线图、饼图、数据轮播、数字翻牌组件

**实现流程**:
1. 封装柱状图组件
2. 封装折线图组件
3. 封装饼图组件
4. 封装数据轮播组件
5. 封装数字翻牌组件

**技术考虑**:
- 组件基于ECharts封装
- 组件配置项统一
- 组件支持主题切换
- 组件响应式适配
- 组件数据更新

**细节考虑**:
- 柱状图：支持堆叠、分组
- 折线图：支持平滑曲线、面积图
- 饼图：支持环形图、玫瑰图
- 数据轮播：自动滚动、暂停
- 数字翻牌：动画效果、千分位

---

### 6.3 大屏模板管理

**功能**: 内置默认模板，支持后台配置大屏展示维度、刷新频率

**实现流程**:
1. 设计大屏模板表结构
2. 内置默认模板
3. 后台支持模板配置
4. 配置项：展示维度、刷新频率、图表类型
5. 前端根据模板渲染大屏

**技术考虑**:
- 模板表：dashboard_templates
- 模板配置JSON格式存储
- 模板切换接口
- 模板预览功能
- 模板导入/导出

**细节考虑**:
- 默认模板：系统概览
- 展示维度：用户、模型、采集、舆情
- 刷新频率：10秒、30秒、60秒
- 图表类型可配置
- 模板启用/禁用

---

### 6.4 全屏与自适应

**功能**: 适配全屏展示、自适应分辨率，风格高级炫酷贴合AI大系统定位

**实现流程**:
1. 全屏按钮功能
2. 分辨率自适应
3. 图表尺寸动态调整
4. 字体大小自适应
5. 布局响应式调整

**技术考虑**:
- 全屏API：requestFullscreen
- 分辨率断点：1920px、1366px、1024px
- 图表resize事件监听
- 字体使用rem单位
- 布局使用flex/grid

**细节考虑**:
- 全屏退出方式（ESC键）
- 分辨率切换平滑过渡
- 图表最小尺寸限制
- 字体最小大小
- 布局不重叠

---

## 七、数智大屏3D增强

### 7.1 3D场景展示

**功能**: 在/dashboard页面增加"3D模式"按钮，点击后全屏展示Three.js场景

**实现流程**:
1. 大屏页面新增"3D模式"按钮
2. 点击按钮后加载Three.js库
3. 初始化3D场景
4. 展示旋转地球仪
5. 标注数据采集点粒子效果

**技术考虑**:
- Three.js本地化引入
- 场景初始化：Scene、Camera、Renderer
- 地球模型：球体+纹理贴图
- 粒子效果：PointCloud
- GPU加速渲染

**细节考虑**:
- 3D模式默认不加载
- 用户点击后才动态加载
- 加载进度提示
- 地球自转动画
- 粒子效果可选

---

### 7.2 性能优化

**功能**: 3D模式默认不加载，提供退出3D模式返回普通大屏的功能

**实现流程**:
1. 3D模式按需加载
2. 动态加载Three.js库
3. 场景资源管理
4. 退出3D模式时释放资源
5. 返回普通大屏

**技术考虑**:
- 动态import加载Three.js
- 场景销毁：dispose()
- 内存泄漏检测
- 帧率监控（≥30fps）
- 降级方案（不支持WebGL时提示）

**细节考虑**:
- 退出按钮位置
- 退出确认提示
- 资源释放完整
- 帧率显示
- 兼容性检测

---

### 7.3 开发限制

**功能**: Three.js本地化，兼容Chrome/Edge最新版

**实现流程**:
1. 下载Three.js本地资源
2. 引入本地Three.js
3. 测试Chrome/Edge兼容性
4. 不强制支持移动端
5. WebGL支持检测

**技术考虑**:
- Three.js版本选择
- 本地资源路径配置
- WebGL支持检测
- 浏览器兼容性测试
- 错误降级处理

**细节考虑**:
- Three.js文件大小
- 加载失败处理
- WebGL不支持提示
- 移动端不强制支持
- 基础地球模型优先

---

## 八、后台仪表盘首页

### 8.1 核心指标卡片

**功能**: 展示注册用户数、今日采集数据量、总采集数据量、模型调用次数、Token总消耗、活跃数字员工数

**实现流程**:
1. 设计指标卡片布局
2. 调用API获取各项指标
3. 渲染卡片（大数字+图标+描述）
4. 支持点击卡片跳转
5. 数据为空时友好提示

**技术考虑**:
- 指标接口：/admin/api/dashboard
- 卡片布局：一行3列或4列
- 图标使用FontAwesome
- 点击跳转路由
- 数据加载状态

**细节考虑**:
- 注册用户数：users表count
- 今日采集数据量：outlook_data今日count
- 总采集数据量：outlook_data总count
- 模型调用次数：model_logs总count
- Token总消耗：model_logs tokens_used总和
- 活跃数字员工数：assistants enabled状态count

---

### 8.2 数据趋势图表

**功能**: 采集数据趋势、Token消耗趋势，支持时间范围切换

**实现流程**:
1. 设计趋势图表区域
2. 调用API获取趋势数据
3. 渲染折线图/柱状图
4. 时间范围切换（今日/本周/本月）
5. 图表数据更新

**技术考虑**:
- 趋势接口：/admin/api/dashboard/trends
- 图表使用本地ECharts
- 时间范围参数
- 数据按天聚合
- 图表自适应

**细节考虑**:
- 采集数据趋势：按天统计outlook_data
- Token消耗趋势：按天统计model_logs
- 时间范围切换按钮
- 图表标题和图例
- 数据为空时提示

---

### 8.3 系统状态与快捷操作

**功能**: 系统状态展示、快捷操作入口

**实现流程**:
1. 设计系统状态区域
2. 展示默认模型名称、定时采集任务状态、数据库文件大小
3. 设计快捷操作区域
4. 快捷操作：添加用户、新建采集任务、测试模型对话、管理数字员工
5. 点击快捷操作跳转

**技术考虑**:
- 系统状态接口：/admin/api/dashboard/status
- 默认模型：models表is_default=1
- 定时任务状态：crawl_schedules enabled count
- 数据库文件大小：SQLite文件size
- 快捷操作路由配置

**细节考虑**:
- 默认模型名称显示
- 定时任务状态：启用/禁用数量
- 数据库文件大小：MB单位
- 快捷操作图标和文字
- 操作权限控制

---

### 8.4 最近动态列表

**功能**: 最近采集任务、最近注册用户展示

**实现流程**:
1. 设计最近动态区域
2. 获取最近采集任务（任务名称、状态、时间）
3. 获取最近注册用户（用户名、注册时间）
4. 列表展示
5. 支持查看更多

**技术考虑**:
- 最近动态接口：/admin/api/dashboard/recent
- 采集任务：outlook_tasks按create_at倒序
- 注册用户：users按create_at倒序
- 限制数量（如各5条）
- 列表样式统一

**细节考虑**:
- 采集任务状态标签
- 用户头像占位
- 时间格式化
- 查看更多链接
- 数据为空时提示

---

### 8.5 API接口

**功能**: 提供/admin/api/dashboard接口，返回所有仪表盘数据

**实现流程**:
1. 设计仪表盘API接口
2. 继承AdminBaseHandler
3. 使用@authenticated保护
4. 返回JSON格式数据
5. 数据聚合处理

**技术考虑**:
- 接口路径：/admin/api/dashboard
- 继承AdminBaseHandler
- @authenticated装饰器
- 返回格式：{code, msg, data}
- 数据分别调用各Model层

**细节考虑**:
- 指标数据：counts
- 趋势数据：trends
- 系统状态：status
- 最近动态：recent
- 错误处理

---

### 8.6 开发限制

**功能**: 遵循开发规范，保证一致性

**技术考虑**:
- 遵循前置任务开发成果及要求
- ECharts本地化，禁止CDN
- 后端统计方法写入对应Model层
- 前端基于Layui布局
- 数据为空时友好占位

**细节考虑**:
- UserRepository：用户统计
- OutlookRepository：采集统计
- ModelRepository：模型统计
- AssistantRepository：员工统计
- 已完成功能不被破坏

---

## 附录：提示词文件索引

| 模块 | 提示词类型 | 文件路径 |
|------|-----------|---------|
| 用户侧认证 | 注册/登录/密码找回 | `app/controllers/auth.py` |
| 瞭望采集增强 | 数据源模板/Header配置 | `app/models/outlook.py` |
| 数据仓库 | 列表/导出/状态标记 | `app/controllers/user/outlook.py` |
| 系统日志 | 操作日志/错误日志 | `app/models/log.py` |
| 统计模块 | 模型/采集/用户统计 | `app/controllers/admin/stats.py` |
| 数字员工 | 管理/新闻/数据助手 | `app/controllers/assistant.py` |
| 数智大屏 | 2D/3D/模板管理 | `app/controllers/dashboard.py` |
| 后台仪表盘 | 指标/趋势/状态 | `app/controllers/admin/dashboard.py` |

---

**文档维护说明**:
- 本文档为协作开发模块提示词汇总
- 与主文档互补，不重复内容
- 新增提示词请及时更新本文档
- 提示词变更需同步更新对应的代码文件
